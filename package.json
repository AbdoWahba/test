{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build --stream",
    "start": "npx lerna run start --scope app"
  },
  "devDependencies": {
    "lerna": "^8.2.3",
    "typescript": "^5.8.3"
  }
}
